# LegacyBridge ULTRA — Python Dependencies
# Install: pip install -r requirements.txt

# ── Web Framework (your original) ────────────────────────────
fastapi>=0.111.0
uvicorn>=0.30.0
websockets>=12.0

# ── Google AI / Gemini (your original) ───────────────────────
google-genai>=0.2.2

# ── Screen Capture (your original) ───────────────────────────
pyautogui>=0.9.54
pillow>=10.2.0

# ── NEW: These were missing — judges need them ────────────────
python-dotenv>=1.0.1        # reads .env file
google-cloud-firestore>=2.16.0  # Firestore state storage
google-cloud-storage>=2.18.0    # GCS training videos
google-cloud-aiplatform>=1.60.0 # Vertex AI / Imagen 3
aiofiles>=23.2.1            # async file operations
httpx>=0.27.0               # async HTTP calls
pyscreenshot>=3.1           # fallback screenshot on Linux
